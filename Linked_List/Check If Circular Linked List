 isCircular(Node *head)
{
    Node *temp=head;
    while(temp->next!=NULL)
    {
        temp=temp->next;
        if(head==temp)
        {
            return 1;
        }
    }
    return 0;
   // Your code here
}
